import React from 'react';
import { Badge } from 'react-native-paper';

const UrgentBadge = () => {
  return (
    <Badge style={{backgroundColor: '#f5bc42'}}>Urgent</Badge>
  );
};

export default UrgentBadge;